# Core
Django>=5.0,<5.2
djangorestframework>=3.15

# Async / queue (DB-backed — no Redis) — D-108
django-q2>=1.6

# Database
psycopg[binary]>=3.1

# Config & security
django-environ>=0.11
cryptography>=42.0                    # Fernet encryption of tenant Meta tokens (SEC-02 / D-100)
djangorestframework-simplejwt>=5.3   # JWT auth for the admin API (D1-11)
django-cors-headers>=4.3             # CORS for the separate React SPA (D-109)

# HTTP (Meta API calls)
requests>=2.31

# Email (handoff) — swap for SendGrid/SES backend in prod
# Uses Django's built-in email framework by default

# Production server
gunicorn>=21.2
